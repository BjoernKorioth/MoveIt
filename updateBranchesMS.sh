
git checkout -b account-management
git pull origin master
git push origin account-management
git checkout master
git branch -D account-management


git checkout -b leaderboard
git pull origin master
git push origin leaderboard
git checkout master
git branch -D leaderboard

git checkout -b rewards
git pull origin master
git push origin rewards
git checkout master
git branch -D rewards

git checkout -b progress
git pull origin master
git push origin progress
git checkout master
git branch -D progress

git checkout -b socialfeed
git pull origin master
git push origin socialfeed
git checkout master
git branch -D socialfeed

git checkout -b goals
git pull origin master
git push origin goals
git checkout master
git branch -D goals

git checkout -b dashboard
git pull origin master
git push origin dashboard
git checkout master
git branch -D dashboard

git checkout -b profile
git pull origin master
git push origin profile
git checkout master
git branch -D profile

git checkout -b information
git pull origin master
git push origin information
git checkout master
git branch -D information

git checkout -b admin-dashboard
git pull origin master
git push origin admin-dashboard
git checkout master
git branch -D admin-dashboard

git checkout -b usergroups
git pull origin master
git push origin usergroups
git checkout master
git branch -D usergroups

git checkout -b staging
git pull origin master
git push origin staging
git checkout master
git branch -D staging

git checkout -b tracking
git pull origin master
git push origin tracking
git checkout master
git branch -D tracking